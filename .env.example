# Configuración opcional de lsxtool (copia a .env y ajusta)
# Ver: lsxtool/servers/cli_modules/provider_config.py

# Modo desarrollo: leer .lsxtool/config del proyecto en lugar de ~/.lsxtool/config
# LSXTOOL_DEV=1

# Ruta explícita al directorio de config (sobreescribe dev y home)
# LSXTOOL_CONFIG_DIR=/path/to/config

# pgx: mirror local de binarios PostgreSQL (arrancar pgx-mirror con scripts/run_pgx_mirror.sh)
# PGX_PG_MIRROR_URL=http://localhost:3737
