# Configuración para dev-registroentidad.yucatan.gob.mx - Traefik 3.4
# Modo LOCAL: Traefik apunta a Apache en puerto 9200 que sirve aplicación
# DocumentRoot: /mnt/d/www/01-STIC/web/registroentidad/app/

http:
  routers:
    # Router HTTP - redirige a HTTPS
    dev-registroentidad-http:
      rule: "Host(`dev-registroentidad.yucatan.gob.mx`)"
      service: apache-9200-registroentidad
      entryPoints:
        - web
      middlewares:
        - redirect-to-https
      priority: 100

    # Router HTTPS - servicio principal
    dev-registroentidad-main:
      rule: "Host(`dev-registroentidad.yucatan.gob.mx`)"
      service: apache-9200-registroentidad
      entryPoints:
        - websecure
      middlewares:
        - security-headers
        - compress
      priority: 100
      tls:
        certResolver: letsencrypt

  services:
    # Servicio Apache local - Puerto 9200
    apache-9200-registroentidad:
      loadBalancer:
        servers:
          - url: "http://localhost:9200"
        passHostHeader: true
        healthCheck:
          path: /
          interval: 30s
          timeout: 5s
          scheme: http
          headers:
            Host: dev-registroentidad.yucatan.gob.mx
